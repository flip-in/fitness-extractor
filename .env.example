# Database Configuration (Local Development)
DB_PASSWORD=postgres_dev_password

# API Configuration
API_KEY=generate-a-random-api-key-here

# Backend Configuration
NODE_ENV=development
PORT=3000
CORS_ORIGIN=http://localhost:5173

# Dashboard Configuration
VITE_API_URL=http://localhost:3000
VITE_MAPBOX_TOKEN=your-mapbox-token-here

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Generate API_KEY with: openssl rand -base64 32
# 3. Get Mapbox token from: https://account.mapbox.com/access-tokens/
# 4. For iOS app, use http://localhost:3000 when testing with simulator
#    or your Mac's local IP (e.g., http://192.168.1.x:3000) for physical device
